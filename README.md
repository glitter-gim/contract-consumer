# contract-consumer

Demo repository consuming platform shared-public contracts.

Setup:

Clone with submodules:
- git clone --recurse-submodules <repo>
- npm install
- npm run typecheck
- npm run run

If already cloned without submodules:
- git submodule update --init --recursive
- npm install
- npm run typecheck
- npm run run
